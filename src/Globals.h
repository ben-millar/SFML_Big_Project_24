#pragma once

#include <iostream>
#include <sfml/Graphics.hpp>

#define WINDOW_HEIGHT sf::VideoMode::getDesktopMode().height / 4 * 3
#define WINDOW_WIDTH WINDOW_HEIGHT
#define RESOLUTION sf::Vector2f{ 1080, 1080 }
#define SCREEN_CENTRE sf::Vector2f{ 540, 540 }
